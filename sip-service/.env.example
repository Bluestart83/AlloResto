# ============================================================
# AlloResto — SIP Bridge Configuration
# Le service vocal (agent IA) est geré par sip-agent-server.
# Ce fichier configure uniquement le SIP Bridge (pjsip).
# ============================================================

# ============================================================
# SIP / TELEPHONIE
# ============================================================
# Credentials SIP (fallback si le restaurant n'a pas sa propre ligne)
SIP_DOMAIN=sip.ovh.fr
SIP_USERNAME=
SIP_PASSWORD=

# TURN/STUN (optionnel, pour NAT traversal)
TURN_SERVER=
TURN_USERNAME=
TURN_PASSWORD=

# ============================================================
# CHIFFREMENT SIP (obligatoire pour les mots de passe en BDD)
# ============================================================
# Generer avec: openssl rand -hex 32
ENCRYPTION_KEY=

# ============================================================
# NEXT.JS (pour decouvrir les agents via /api/sip/agents)
# ============================================================
NEXT_API_URL=http://localhost:3000

# ============================================================
# REDIS (pour push les appels entrants au worker)
# ============================================================
REDIS_URL=redis://localhost:6379

# ============================================================
# SIP BRIDGE
# ============================================================
BRIDGE_PORT=5060
